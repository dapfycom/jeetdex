import bounding from '@/assets/abis/bonding.abi.json';
import abi from '@/assets/abis/pair-full.abi.json';
export const siteModes = ['normie', 'degen'];

export const pairContractAbi = abi;

export const bondingContractAbi = bounding;
