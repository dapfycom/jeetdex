import CreatePoolsContainer from '@/components/CreatePool/CreatePoolsContainer';

const page = () => {
  return (
    <div className='my-10 mx-auto max-w-xl container'>
      <CreatePoolsContainer />
    </div>
  );
};

export default page;
