import { Button } from '@/components/ui/button';

const PlaceTradeButton = () => {
  return (
    <Button className=' dark:focus-visible:ring-slate-300 dark:bg-slate-50 dark:text-slate-900 dark:hover:bg-slate-50/90 h-10 w-full mt-3'>
      place trade
    </Button>
  );
};

export default PlaceTradeButton;
